"use client";
import { ChangeEvent, useEffect, useMemo, useState } from "react";
import { loadClassStudents, loadPupilLearningContext, loadPupilPortfolio, loadTeacherWorkspace, saveLiveEvidence, type LiveClass, type LiveStudent, type PupilLearningContext, type PupilPortfolio } from "../../lib/sportfolio/live";
import { supabase } from "../../lib/supabase/client";
import "./live.css";

type Tag={id:string;name:string;category:string};
type Workspace={classes:LiveClass[];activeClass?:LiveClass;students:LiveStudent[];tags:Tag[]};
type View="classes"|"class"|"capture"|"portfolio";

export default function LiveWorkspace(){
 const [workspace,setWorkspace]=useState<Workspace|null>(null),[activeClass,setActiveClass]=useState<LiveClass|null>(null),[students,setStudents]=useState<LiveStudent[]>([]),[view,setView]=useState<View>("classes"),[selectedStudents,setSelectedStudents]=useState<string[]>([]),[selectedTags,setSelectedTags]=useState<string[]>([]),[note,setNote]=useState(""),[nextStep,setNextStep]=useState(""),[requestReflection,setRequestReflection]=useState(false),[file,setFile]=useState<File|null>(null),[previewUrl,setPreviewUrl]=useState<string|null>(null),[status,setStatus]=useState<"loading"|"ready"|"saving"|"saved"|"error">("loading"),[message,setMessage]=useState(""),[context,setContext]=useState<PupilLearningContext|null>(null),[contextLoading,setContextLoading]=useState(false),[portfolio,setPortfolio]=useState<PupilPortfolio|null>(null),[portfolioLoading,setPortfolioLoading]=useState(false);
 useEffect(()=>{loadTeacherWorkspace().then(data=>{const w=data as Workspace;setWorkspace(w);setActiveClass(w.activeClass??w.classes[0]??null);setStudents(w.students??[]);setStatus("ready")}).catch(error=>{setMessage(error instanceof Error?error.message:"Unable to load Sportfolio.");setStatus("error")})},[]);
 useEffect(()=>()=>{if(previewUrl)URL.revokeObjectURL(previewUrl)},[previewUrl]);
 useEffect(()=>{if(view!=="capture"||selectedStudents.length!==1){setContext(null);return}setContextLoading(true);loadPupilLearningContext(selectedStudents[0]).then(setContext).catch(()=>setContext(null)).finally(()=>setContextLoading(false))},[selectedStudents,view]);
 const selectedNames=useMemo(()=>students.filter(s=>selectedStudents.includes(s.id)),[students,selectedStudents]);
 function toggleStudent(id:string){setSelectedStudents(c=>c.includes(id)?c.filter(v=>v!==id):[...c,id])} function toggleTag(id:string){setSelectedTags(c=>c.includes(id)?c.filter(v=>v!==id):[...c,id])}
 async function openClass(item:LiveClass){setStatus("loading");setActiveClass(item);setMessage("");try{const list=await loadClassStudents(item.id);setStudents(list);setSelectedStudents([]);setView("class");setStatus("ready")}catch(error){setMessage(error instanceof Error?error.message:"Unable to load class.");setStatus("error")}}
 function openCapture(){if(!activeClass)return;setSelectedStudents([]);setSelectedTags([]);setMessage("");setStatus("ready");setView("capture")}
 async function openPortfolio(student:LiveStudent){setPortfolioLoading(true);setPortfolio(null);setMessage("");setView("portfolio");try{setPortfolio(await loadPupilPortfolio(student.id))}catch(error){setMessage(error instanceof Error?error.message:"Unable to load pupil portfolio.")}finally{setPortfolioLoading(false)}}
 function chooseFile(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0]??null;if(previewUrl)URL.revokeObjectURL(previewUrl);setFile(f);setPreviewUrl(f?URL.createObjectURL(f):null);setMessage("");if(status==="saved"||status==="error")setStatus("ready")}
 function clearFile(){if(previewUrl)URL.revokeObjectURL(previewUrl);setFile(null);setPreviewUrl(null)}
 async function save(){if(!activeClass)return;setStatus("saving");setMessage("");try{const id=await saveLiveEvidence({classId:activeClass.id,studentIds:selectedStudents,tagIds:selectedTags,title:file?file.name.replace(/\.[^.]+$/,""):"Quick capture",teacherNote:note,nextStep,requestReflection,file});setStatus("saved");setMessage(`Evidence saved securely · ${id.slice(0,8)}`);setSelectedStudents([]);setSelectedTags([]);setNote("");setNextStep("");setRequestReflection(false);clearFile()}catch(error){setStatus("error");setMessage(error instanceof Error?error.message:"Could not save evidence.")}}
 async function signOut(){await supabase.auth.signOut();window.location.replace("/login")}
 if(status==="loading"&&!workspace)return <main className="live-loading">Loading Sportfolio…</main>; if(!workspace)return <main className="live-loading"><div><h1>Sportfolio</h1><p>{message}</p><a href="/login">Sign in</a></div></main>;
 const isVideo=file?.type.startsWith("video/"),isImage=file?.type.startsWith("image/");
 return <main className="live-shell"><aside className="live-sidebar"><div className="live-brand"><span>S</span><strong>SPORTFOLIO</strong></div><nav><button className={view==="classes"?"active":""} onClick={()=>setView("classes")}>▣ Classes</button><button className={view==="class"||view==="portfolio"?"active":""} onClick={()=>activeClass&&setView("class")}>◎ Pupils</button><button className={view==="capture"?"active capture-nav":"capture-nav"} onClick={openCapture}>● Capture</button></nav><button className="signout" onClick={signOut}>Sign out</button></aside><section className="live-content"><header><div><small>TEACHER PILOT</small><strong>{activeClass?.name??"My Sportfolio"}</strong></div><div className="live-badge">● Connected</div></header>{view==="classes"&&<ClassesView classes={workspace.classes} onOpen={openClass}/>} {view==="class"&&activeClass&&<ClassView activeClass={activeClass} students={students} onBack={()=>setView("classes")} onCapture={openCapture} onPortfolio={openPortfolio}/>} {view==="portfolio"&&<PortfolioView portfolio={portfolio} loading={portfolioLoading} error={message} activeClass={activeClass} onBack={()=>setView("class")} onCapture={openCapture}/>} {view==="capture"&&activeClass&&<div className="live-page"><div className="live-heading"><div><button className="back-link" onClick={()=>setView("class")}>← {activeClass.name}</button><span className="eyebrow-orange">QUICK CAPTURE</span><h1>Capture the moment.</h1><p>Select the pupils, tag what you saw, and move on.</p></div><div className="class-chip">{activeClass.activity??"PE"}<b>{activeClass.academic_year}</b></div></div><div className="live-grid"><section className="camera-card"><div className={`camera-view ${previewUrl?"has-preview":""}`}>{previewUrl&&isVideo&&<video src={previewUrl} controls playsInline preload="metadata"/>}{previewUrl&&isImage&&<img src={previewUrl} alt="Selected evidence preview"/>}{!previewUrl&&<><span className="camera-label">READY TO CAPTURE</span><div className="camera-focus">+</div><div className="camera-prompt">Add a photo or video</div></>}{file&&<div className="file-pill"><span>{file.type.startsWith("video/")?"VIDEO":"PHOTO"}</span>{file.name}<button onClick={clearFile} aria-label="Remove media">×</button></div>}</div><div className="camera-controls"><label className="capture-source">▧ Photo<input type="file" accept="image/jpeg,image/png,image/heic" capture="environment" onChange={chooseFile}/></label><label className="record"><span></span><input type="file" accept="video/mp4,video/quicktime" capture="environment" onChange={chooseFile}/></label><label className="capture-source selected-mode">▣ Video<input type="file" accept="video/mp4,video/quicktime" capture="environment" onChange={chooseFile}/></label></div><p>{file?`${(file.size/1024/1024).toFixed(1)} MB · will be stored privately in Sportfolio`:"On iPad or phone, Photo and Video open the device camera. Desktop opens the file picker."}</p></section><section className="live-panel"><div className="panel-block"><div className="panel-title"><h2>Who?</h2><span>{selectedStudents.length} selected</span></div>{students.length?<div className="student-picker">{students.map(student=>{const selected=selectedStudents.includes(student.id),initials=`${student.first_name[0]??""}${student.last_name?.[0]??""}`;return <button key={student.id} className={selected?"student-choice selected":"student-choice"} onClick={()=>toggleStudent(student.id)}><span>{initials}</span><strong>{student.first_name}</strong>{selected&&<b>✓</b>}</button>})}</div>:<p className="empty-copy">No pupils are in this class yet.</p>}</div>{selectedStudents.length===1&&<div className="learning-context"><div className="context-head"><div><small>LEARNING HISTORY</small><strong>{selectedNames[0]?.first_name}</strong></div>{context&&<span>{context.evidenceCount} evidence</span>}</div>{contextLoading?<p>Loading previous learning…</p>:context?<><div className="context-next"><small>CURRENT NEXT STEP</small><strong>{context.nextSteps[0]?.final_body??context.activeGoals[0]?.body??"No next step recorded yet."}</strong></div>{context.recentEvidence[0]&&<div className="context-last"><small>LAST OBSERVATION</small><span>{context.recentEvidence[0].teacher_note||context.recentEvidence[0].title||"Evidence captured"}</span><div>{context.recentEvidence[0].tags.map(tag=><b key={tag}>{tag}</b>)}</div></div>}</>:<p>No previous learning context yet.</p>}</div>}<div className="panel-block"><div className="panel-title"><h2>What does it show?</h2><span>{selectedTags.length} tags</span></div><div className="live-tags">{workspace.tags.map(tag=><button key={tag.id} className={selectedTags.includes(tag.id)?"active":""} onClick={()=>toggleTag(tag.id)}>{tag.name}</button>)}</div></div><div className="panel-block"><label className="note-label">Quick note<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Optional teacher observation…"/></label><label className="note-label">Next learning step<textarea value={nextStep} onChange={e=>setNextStep(e.target.value)} placeholder="What should these pupils focus on next?"/></label><label className="reflection-toggle"><input type="checkbox" checked={requestReflection} onChange={e=>setRequestReflection(e.target.checked)}/><span><strong>Request pupil reflection</strong><small>{nextStep.trim()?"Reflection will ask the pupil how they will act on this next step.":"Creates a reflection task for every selected pupil."}</small></span></label></div><div className="save-zone"><div className="selected-strip">{selectedNames.length?selectedNames.map(s=><span key={s.id}>{s.first_name}</span>):<em>Select pupils to continue</em>}</div><button className="save-live" disabled={!selectedStudents.length||status==="saving"} onClick={save}>{status==="saving"?(file?"Uploading + saving…":"Saving securely…"):"Save evidence"}</button>{message&&<div className={`live-message ${status}`}>{message}</div>}{status==="saved"&&<button className="review-after-save" onClick={()=>setView("class")}>Review pupil portfolios</button>}</div></section></div></div>}</section></main>
}

function ClassesView({classes,onOpen}:{classes:LiveClass[];onOpen:(item:LiveClass)=>void}){return <div className="live-page classes-page"><div className="live-heading"><div><span className="eyebrow-orange">MY SPORTFOLIO</span><h1>Your classes</h1><p>Choose a class to capture evidence or review pupil portfolios.</p></div></div><div className="class-grid">{classes.map(item=><button key={item.id} className="class-card" onClick={()=>onOpen(item)}><div><h2>{item.name}</h2><p>{item.activity??"PE"}</p></div><span>{item.pupil_count??0} pupil{item.pupil_count===1?"":"s"}</span><b>Open →</b></button>)}</div></div>}

function ClassView({activeClass,students,onBack,onCapture,onPortfolio}:{activeClass:LiveClass;students:LiveStudent[];onBack:()=>void;onCapture:()=>void;onPortfolio:(student:LiveStudent)=>void}){return <div className="live-page class-workspace"><div className="workspace-top"><div><button className="back-link" onClick={onBack}>← Your classes</button><span className="eyebrow-orange">CLASS WORKSPACE</span><h1>{activeClass.name}</h1><p>{activeClass.activity??"PE"} · {students.length} pupil{students.length===1?"":"s"}</p></div><button className="primary-capture" onClick={onCapture}>● Capture evidence</button></div><div className="workspace-section"><div className="section-head"><div><h2>Pupils</h2><p>Tap a pupil to review their Sportfolio.</p></div></div>{students.length?<div className="pupil-review-grid">{students.map(student=><button className="pupil-review-card" key={student.id} onClick={()=>onPortfolio(student)}><span className="pupil-avatar">{student.first_name[0]}{student.last_name?.[0]??""}</span><div><strong>{student.first_name} {student.last_name??""}</strong><small>{student.grade??activeClass.name}</small></div><b>View Sportfolio →</b></button>)}</div>:<div className="empty-state"><h3>No pupils yet</h3><p>Add pupils to this class before capturing evidence.</p></div>}</div></div>}

function PortfolioView({portfolio,loading,error,activeClass,onBack,onCapture}:{portfolio:PupilPortfolio|null;loading:boolean;error:string;activeClass:LiveClass|null;onBack:()=>void;onCapture:()=>void}){if(loading)return <div className="live-page portfolio-page"><button className="back-link" onClick={onBack}>← Back to class</button><div className="empty-state"><p>Loading pupil Sportfolio…</p></div></div>;if(!portfolio)return <div className="live-page portfolio-page"><button className="back-link" onClick={onBack}>← Back to class</button><div className="empty-state"><h3>Could not load this Sportfolio</h3><p>{error}</p></div></div>;const s=portfolio.student;return <div className="live-page portfolio-page"><div className="workspace-top"><div><button className="back-link" onClick={onBack}>← {activeClass?.name??"Class"}</button><span className="eyebrow-orange">PUPIL SPORTFOLIO</span><div className="portfolio-title"><span className="pupil-avatar large">{s.first_name[0]}{s.last_name?.[0]??""}</span><div><h1>{s.first_name} {s.last_name??""}</h1><p>{s.grade??activeClass?.name} · {portfolio.evidenceCount} evidence item{portfolio.evidenceCount===1?"":"s"}</p></div></div></div><button className="primary-capture" onClick={onCapture}>● Capture for class</button></div>{(portfolio.currentNextStep||portfolio.currentGoal)&&<div className="next-step-banner"><small>CURRENT LEARNING PRIORITY</small><strong>{portfolio.currentNextStep??portfolio.currentGoal}</strong></div>}<div className="timeline-list">{portfolio.items.length?portfolio.items.map(item=><article className="portfolio-item" key={item.id}>{item.media.map(media=>media.signed_url&&(media.media_type==="image"?<img className="portfolio-media" src={media.signed_url} alt="Private pupil evidence" key={media.id}/>:media.media_type==="video"?<video className="portfolio-media" src={media.signed_url} controls playsInline preload="metadata" key={media.id}/>:<audio src={media.signed_url} controls key={media.id}/>))}<div className="portfolio-copy"><div className="portfolio-meta">{new Date(item.occurred_at).toLocaleDateString()} · {item.class_name??activeClass?.name}</div><h2>{item.title||"Evidence"}</h2>{item.tags.length>0&&<div className="portfolio-tags">{item.tags.map(tag=><span key={tag}>{tag}</span>)}</div>}{item.teacher_note&&<p>{item.teacher_note}</p>}{item.next_step&&<div className="item-next"><small>NEXT STEP</small><strong>{item.next_step}</strong></div>}{item.reflection&&<div className="reflection-card"><small>PUPIL REFLECTION</small><p>{item.reflection.text_response??item.reflection.prompt??"Reflection requested"}</p></div>}{item.student_feedback&&<div className="reflection-card"><small>FEEDBACK</small><p>{item.student_feedback}</p></div>}</div></article>):<div className="empty-state"><h3>No evidence yet</h3><p>Capture the first piece of evidence for {s.first_name}.</p><button className="primary-capture" onClick={onCapture}>● Capture evidence</button></div>}</div></div>}
